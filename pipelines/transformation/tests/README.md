Singular tests (custom SQL assertions that don't fit the generic
not_null/unique/accepted_values/relationships pattern) go here as .sql files.
None needed yet — all current checks are covered by generic tests in the
`_*.yml` schema files next to each model.
