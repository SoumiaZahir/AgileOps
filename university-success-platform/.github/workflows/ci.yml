name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  code-quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Linting & Formatting Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff black

      - name: Run Ruff Linter
        run: |
          ruff check .

      - name: Run Black Formatter Check
        run: |
          black --check .

      # TODO: Integrate DBT Tests for Data Transformation quality check (from Marouane)
      - name: Future Gate - DBT Tests (Marouane)
        run: |
          echo "Placeholder (Marouane): Running dbt test for data transformations..."

      # TODO: Integrate Great Expectations for Data Quality validation (from Imane)
      - name: Future Gate - Great Expectations Data Quality (Imane)
        run: |
          echo "Placeholder (Imane): Running Great Expectations validation checkpoint..."

      # TODO: Integrate pytest for FastAPI unit and integration tests (from Houssame)
      - name: Future Gate - Pytest for FastAPI (Houssame)
        run: |
          echo "Placeholder (Houssame): Running pytest suite for FastAPI endpoints..."
